<%= form_for(@course_pack) do |f| %>
  <% if @course_pack.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course_pack.errors.count, "error") %> prohibited this course_pack from being saved:</h2>

      <ul>
      <% @course_pack.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
